services:
  - type: web
    name: structura-api
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: CORS_ORIGINS
        value: "https://dhruviw.github.io,http://localhost:5173"
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
